@{
    ViewBag.Title = "Index";
}
@using MyLibrary.Model.ViewModel;
@model IEnumerable<MyLibrary.Model.Domain.T_Book>
@using MyLibrary.Web.Auth
@section header{
    <script type="text/javascript" src="~/Scripts/jquery.lazyload.min.js"></script>
    <script type="text/javascript">
        $(function () {
            $("img.lazyloading").lazyload();
        });
         
        $(function () {
            $("img.lazyloading").lazyload();
        });
        function check() {
            var content = $(".form-control").val();
            //var type;
            //if ($("#Radio1").attr("checked"))
            //{
            //    type = "关键词";
            //}
            //if ($("#Radio2").attr("checked"))
            //{
            //    type = "作者";

            //}
            //if ($("#Radio3").attr("checked")) {
            //    type = "ISBN";

            //}
            //if ($("#Radio4").attr("checked")) {
            //    type = "类别号";

            //}
            $("#conCheck").submit();


        }
</script>
}
<div class="wrapper clearfix">
    <!-- main -->
    @*
    <div class="main">
        @Html.Action("CategorySummary", "Home", null)
    </div>
    *@

</div>
@* 图书检索*@
<div>
    <form method="post" action="~/home/index" id="conCheck">
        <input type="text" name="content" class="form-control" placeholder="检索内容" style="display:inline;">
        <button type="button" class="btn btn-primary btn-lg active" onclick="check()">检索</button>
        <br />
        @*<input id="Radio1" type="radio" name="type" checked="checked"  value="关键词"/>关键词
        <input id="Radio2" type="radio" name="type"  value="作者"/>作者
        <input id="Radio3" type="radio" name="type"  value="ISBN"/>ISBN
        <input id="Radio4" type="radio" name="type" value="类别号" />类别号*@
        @Html.RadioButton("type","关键词",true)<span>关键词</span>
        @Html.RadioButton("type", "作者")<span>作者</span>
        @Html.RadioButton("type", "ISBN")<span>ISBN</span>
        @Html.RadioButton("type", "类别号")<span>类别号</span>
        

        <input type="hidden"  />
    </form>
</div>
@*查询显示*@
<div class="wrapper section-lists">
    <!-- list-inner -->
    <div class="list-inner clearfix">
        <table class="table table-hover">
            <tr>
                <td>封面</td>
                <td>书名</td>
                <td>ISBN</td>
                <td>作者</td>
                <td width="300">简介</td>
                <td>库存</td>
            </tr>
           @foreach (var item in Model)
            {
                <tr>
                   <td><img src="@Html.DisplayFor(modelItem => item.BookCover.Large)" style="width:120px; height:150px"/></td>
                    <td>@Html.DisplayFor(modelItem => item.BookName)</td>
                    <td>@Html.DisplayFor(modelItem => item.ISBN13)</td>
                    <td>@Html.DisplayFor(modelItem => item.Author)</td>
                    <td width="300">@Html.DisplayFor(modelItem => item.Summary)</td>
                    <td>@Html.DisplayFor(modelItem=>item.BookNum)</td>
                </tr>
            }
           
            @*@foreach (hotBooks item in ViewBag.hot)
            {
                <tr>
                    <td><img src="@Html.DisplayFor(modelItem =>item.Book.BookCover)" style="width:120px; height:150px" /></td>
                    <td>@Html.DisplayFor(modelItem => item.Book.BookName)</td>
                    <td>@Html.DisplayFor(modelItem => item.Book.ISBN13)</td>
                    <td>@Html.DisplayFor(modelItem => item.Book.Author)</td>
                    <td width="300">@Html.DisplayFor(modelItem => item.Book.Summary)</td>
                    <td>@Html.DisplayFor(modelItem => item.Book.BookNum)</td>
                </tr>
            }*@
        </table>
    </div>

</div>